# Legacy Archive

This folder holds local archive snapshots that were confirmed to be unused by the
active AstraForge application during the 2026-05-16 cleanup pass.

The current archived snapshot is intentionally kept out of the active import/test
path so it cannot shadow the running codebase or duplicate the active test suite.
